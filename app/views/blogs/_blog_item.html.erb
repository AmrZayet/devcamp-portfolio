<!-- This is created in Rails 7 to follow the video (this content should have been at /views/blogs/_blog.html.erb)
In order to make this work like the video but without missing the rails 7 app, I created this: -->
<tr>
  <td><%= blog_item.title %></td>
  <td><%= blog_item.body %></td>
  <td><%= link_to blog_item.status, toggle_status_blog_path(blog_item) %></td>
  <td><%= link_to 'Show', blog_item %></td>
  <td><%= link_to 'Edit', edit_blog_path(blog_item) %></td>
  <td><%= link_to 'Delete Post', blog_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>